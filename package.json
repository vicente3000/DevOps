{
  "name": "personas-api-devops",
  "version": "1.0.0",
  "description": "API backend simple para guardar personas en memoria.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "prisma:generate": "prisma generate --schema=src/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate deploy --schema=src/prisma/schema.prisma"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "api",
    "devops",
    "node"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "6"
  },
  "devDependencies": {
    "prisma": "6"
  }
}